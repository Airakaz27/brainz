<div class="container">
  <div class="confirmation">
    <div class="thankyou">
      <h1>Thank you <%= @user.pseudo %></h1>
      <p>your reservation of <strong><%= @brain.name %></strong> will be final after validation by the owner...</p>
    </div>

    <div class="booking-date-confirmation">
      <div class="booking-date-infos">
        <p><strong>Start date : </strong><%= @booking.start_date %></p>
        <p><strong>End date : </strong><%= @booking.end_date %></p>
        <p><strong>Price : </strong><%= @booking.total_price %> €</p>
      </div>
    </div>
  </div>
  <div class="btn-back">
    <%= link_to "Back to your dashboard", user_path(@user), class: "main-button" %>
  </div>
</div>
